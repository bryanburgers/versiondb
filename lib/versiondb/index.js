'use strict';

const initialize = require('./initialize');
const inventory = require('./inventory');
const upgrade = require('./upgrade');

module.exports = {
    initialize,
    inventory,
    upgrade,
};
